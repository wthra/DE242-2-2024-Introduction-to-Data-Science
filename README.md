# DE242-2-2024-Introduction-to-Data-Science
**  Deepfake Audio Detection  **


Code  URL

https://colab.research.google.com/drive/1Gr3POyqnngYd5Juqg11uf4pK__uCJRRU?usp=sharing

Blog  URL

https://medium.com/@phattharamongkon/deepfake-audio-detection-4ec8862b209b

CANVA URL

https://www.canva.com/design/DAGinobJtQI/ebmqZiGv59vKTSpZKrWyGA/edit?utm_content=DAGinobJtQI&utm_campaign=designshare&utm_medium=link2&utm_source=sharebutton




# 🎙️ Deepfake Audio Detection using CNN + BiLSTM

[Deepfake Audio]
โปรเจกต์นี้มีจุดประสงค์เพื่อพัฒนาโมเดล Deep Learning สำหรับตรวจจับเสียงสังเคราะห์ (Deepfake Audio / AI-Generated Speech) โดยใช้สถาปัตยกรรมแบบผสมผสานระหว่าง **CNN (Convolutional Neural Networks)** และ **Bi-LSTM (Bidirectional Long Short-Term Memory)** เพื่อวิเคราะห์คุณลักษณะของเสียงทั้งในเชิงพื้นที่ (Spatial) และเชิงเวลา (Temporal)

## 📌 สารบัญ
- [ที่มาและแนวคิด (Overview)](#-ที่มาและแนวคิด-overview)
- [กระบวนการทำงาน (Methodology)](#-กระบวนการทำงาน-methodology)
- [โครงสร้างโมเดล (Model Architecture)](#-โครงสร้างโมเดล-model-architecture)
- [ผลลัพธ์การทดลอง (Results)](#-ผลลัพธ์การทดลอง-results)
- [การติดตั้งและใช้งาน (Installation & Usage)](#-การติดตั้งและใช้งาน-installation--usage)

---

## 🧐 ที่มาและแนวคิด (Overview)
ปัจจุบันเทคโนโลยีการสังเคราะห์เสียง (Text-to-Speech / Voice Cloning) มีความสมจริงมากจนยากที่จะแยกแยะด้วยหูเปล่า โปรเจกต์นี้จึงนำเสนอวิธีการตรวจสอบโดยแปลงไฟล์เสียงให้เป็นรูปภาพ (**Mel-Spectrogram**) แล้วใช้ AI วิเคราะห์ความผิดปกติของคลื่นเสียงที่มนุษย์อาจไม่ได้ยิน

**เป้าหมาย:** แยกแยะระหว่างเสียงมนุษย์จริง (**Real**) และเสียงที่สร้างโดย AI (**Fake**)

---

## ⚙️ กระบวนการทำงาน (Methodology)

### 1. Data Preprocessing & Augmentation
ก่อนนำเข้าโมเดล ข้อมูลเสียงจะถูกเตรียมดังนี้:
* **Feature Extraction:** ใช้ `librosa` แปลงไฟล์เสียง (.wav/.flac) เป็น **Mel-Spectrogram** (แปลงเสียงเป็นภาพความถี่)
* **Normalization:** แปลงค่า Amplitude เป็น Decibels (dB scale)
* **Data Augmentation:** เพิ่มความหลากหลายของข้อมูลเพื่อป้องกัน Overfitting (เช่น การเติม Noise หรือปรับความเร็ว - *ตามโค้ดที่มีการระบุ Augmentation*)

### 2. Model Input
ข้อมูลที่เข้าสู่โมเดลจะมีลักษณะเป็นรูปภาพ 3 มิติ: `(Time Steps, Frequency Bins, Channels)`

---

## 🧠 โครงสร้างโมเดล (Model Architecture)

เราใช้โมเดลแบบ Hybrid ที่ประกอบด้วย:

1.  **TimeDistributed CNN:**
    * ทำหน้าที่สกัด Feature จาก Mel-Spectrogram ในแต่ละช่วงเวลา
    * ช่วยจับ pattern ของความถี่เสียงที่ผิดปกติ (Artifacts) ที่มักเกิดในเสียงสังเคราะห์
2.  **Bi-LSTM (Bidirectional LSTM):**
    * รับ Feature ที่ได้จาก CNN มาวิเคราะห์ความต่อเนื่องทางเวลา (Temporal Dependency)
    * การใช้ Bi-LSTM ช่วยให้มองเห็นบริบททั้ง "อดีต" และ "อนาคต" ของลำดับเสียง ทำให้แม่นยำกว่า LSTM ปกติ
3.  **Classification Layer:**
    * **Dense Layer** พร้อม Dropout (เพื่อลด Overfitting)
    * **Output Node:** Sigmoid function (ให้ค่าความน่าจะเป็น 0-1)

> **สรุป Flow:** Audio $\rightarrow$ Mel-Spectrogram $\rightarrow$ CNN (Extract Features) $\rightarrow$ BiLSTM (Analyze Sequence) $\rightarrow$ Real/Fake

---

## 📊 ผลลัพธ์การทดลอง (Results)

จากการเทรนและทดสอบโมเดล ได้ผลลัพธ์ดังนี้:

| Metric | รายละเอียด |
|:---:|:---|
| **Accuracy** | *[ใส่ค่า Accuracy ที่ได้ เช่น 95.5%]* |
| **Precision** | ความแม่นยำในการทำนายว่าเป็นเสียงปลอม |
| **Recall** | ความสามารถในการดึงข้อมูลเสียงปลอมทั้งหมดออกมาได้ |
| **F1-Score** | ค่าเฉลี่ย Harmonic ของ Precision และ Recall |

*(คุณสามารถนำรูป Confusion Matrix หรือกราฟ Loss/Accuracy ที่ได้จาก Notebook มาแปะตรงนี้เพื่อความสมบูรณ์)*

---

## 💻 การติดตั้งและใช้งาน (Installation & Usage)

### 1. Requirements
โปรเจกต์นี้รันบน Python โดยใช้ Library หลักดังนี้:
```bash
pip install tensorflow librosa numpy matplotlib seaborn scikit-learn
```

2. การเตรียมข้อมูล (Dataset Structure)
จัดเตรียมโฟลเดอร์ข้อมูลเสียงดังนี้:
```
dataset/
    ├── real/  # เก็บไฟล์เสียงจริง
    └── fake/  # เก็บไฟล์เสียง AI
```

3. การใช้งาน (How to Run)
เปิดไฟล์ Deepfake_Train_CNN_BiLSTM_Augmentation.ipynb

รันเซลล์เพื่อทำการ Preprocess ข้อมูลและเทรนโมเดล

การทำนายผล (Inference): ใช้โค้ดส่วนท้ายของ Notebook เพื่อทดสอบไฟล์เสียงใหม่
```
# ตัวอย่างโค้ดทำนายผล
import librosa
import numpy as np

# Load and process audio
y, sr = librosa.load("test_audio.wav", sr=16000)
mel_spectrogram = librosa.feature.melspectrogram(y=y, sr=sr)
mel_spectrogram_db = librosa.power_to_db(mel_spectrogram, ref=np.max)
mel_input = mel_spectrogram_db[..., np.newaxis] # Reshape

# Predict
prediction = model.predict(np.expand_dims(mel_input, axis=0))
print("Fake" if prediction < 0.5 else "Real")
```
Credits
พัฒนาโดยใช้ TensorFlow/Keras

